# QA — Ronda total (2026-07-27)

Rama `brotherhood-publico`. Sistema real de Brotherhood (Supabase de producción, el mismo que sirve `brotherhood-xi.vercel.app`), dos sedes: **San Diego** `3d8a8527…` y **Viñedo** `04fb974d…`.

Encargo: **probar usando el sistema**, no leyéndolo. Todo lo que sigue son ciclos cerrados — montar el dato por la API real, ejercitarlo, medir antes/después en la BD *y* en el endpoint que ve el humano, limpiar y confirmar que quedó limpio.

**Resultado en una línea:** 182 comprobaciones automatizadas, **179 pasan**. No apareció ninguna fuga entre sedes ni ninguna pérdida de dinero. De los hallazgos, **los dos que rompían algo ya están arreglados**; quedan dos validaciones que solo existen en el navegador y dos huecos conocidos.

**Verificación de código al cerrar:** `tsc` limpio · `lint` en 0 · **543 tests** (los 535 de línea base + 8 nuevos: 2 del fitness del marcador y 6 del freno por mesa).

> **Segunda pasada.** Con la confirmación de que los datos son de demo, volví sobre las dos cosas que había dejado sin probar: el **cierre del día real** (21/21, con restauración exacta del estado) y el **descuento de inventario con el flag encendido** (19/19, devolviendo la configuración a su valor original). Además se arregló el freno de "Pedir la cuenta" y se limpió la base de Pizzería 007. Todo eso está incorporado abajo.

---

## 0. Lo primero: ¿qué está en vivo?

| | |
|---|---|
| `public/sw.js` en la rama | `santo-static-v22` |
| `https://brotherhood-xi.vercel.app/sw.js` | `santo-static-v22` |

**Coinciden.** Lo desplegado es el código de `30a3aa7`, así que el paquete de cuentas abiertas y el lote de pagos sí están en la calle. No hizo falta publicar antes de empezar.

### Estado real de las migraciones (verificado en la BD, no en el repo)

`npm run qa:db-state` consulta la base viva: columnas reales, RLS probado con la *anon key*, y el bucket de comprobantes.

| Migración | Estado | Cómo se comprobó |
|---|---|---|
| `0030` segunda imagen de comprobante | **APLICADA** | las tres columnas existen |
| `0031` `orders.payment_method` | **APLICADA** | la columna existe |
| `0032` RLS del esquema | **APLICADA** | la anon key recibe 0 filas de `orders`, `payment_proofs` y `order_branch_counters` |
| `0032` bucket `payment-proofs` privado | **APLICADA** | `public=false` |
| `0034` RLS de pagos a proveedores | **APLICADA** | la anon key recibe 0 filas |
| `0033`/`0035` índice de cuenta abierta por sede | **APLICADA** | la BD rechazó con `23505` un intento de abrir una segunda cuenta de la misma mesa escrita `"  MESA 1  "` — o sea, el índice **normalizado** de la 0035 está vivo |

No queda ninguna migración pendiente de este lote.

---

## 1. Un incidente que hay que contar primero

La primera corrida de la línea base salió con 7 fallas repartidas entre `supplier-payables` y `order-idempotency`. No eran del código: **los scripts e2e apuntan a `localhost:3000` por defecto, y en ese puerto estaba levantado el dev server de otro cliente (Pizzería 007, que ya está en producción).** Los e2e le crearon pedidos, proveedores, compras y un insumo *a su base de datos real*, y su limpieza borró en el Supabase de Brotherhood, así que allá quedaron huérfanos.

Residuos que quedaron en la BD de Pizzería 007 (verificados uno a uno, solo lectura):

- `orders`: `SMOKE-PAGO`, `SMOKE-B-PED`, `SMOKE-FISCAL`, `IDEM-E2E` (+ 5 filas en `order_items`)
- `suppliers`: `SMOKE-…-PROV`, `PAYABLE-1785126341417-PROV`, `PAYABLE-1785126527755-PROV`
- `supplier_purchases`: las 3 compras de esos proveedores
- `inventory_items`: `SMOKE-…-HARINA`

**Ya está limpio.** Con el visto bueno ejecuté la limpieza: **18 filas borradas** (4 pedidos, 5 líneas de pedido, 3 proveedores, 3 compras, 2 movimientos de inventario y 1 insumo) y verificación final en **0 residuos** en las cuatro tablas. El script de un solo uso se borró después.

Para que no vuelva a pasar, la librería común de esta ronda (`scripts/qa-lib.mjs`) tiene dos defensas: el puerto por defecto es **3177**, y `assertBrotherhood()` lee el `<title>` del servidor y **aborta** si no dice Brotherhood.

Con el servidor correcto, la línea base queda limpia:

| Script | Resultado |
|---|---|
| `npm run smoke` | 5 OK, 0 fallas |
| `npm run e2e:purchase-inventory` | 9 OK, 0 fallas |
| `npm run e2e:supplier-payables` | 8 OK, 0 fallas |
| `npm run e2e:reports-2e` | 7 OK, 0 fallas |
| `npm run e2e:order-idempotency` | 7 OK, 0 fallas |
| `npm run e2e:business-complexity` | 3 OK, 0 fallas |
| `npx tsc --noEmit` | limpio |
| `npx vitest run` | 535 tests, 76 archivos |
| `npm run lint` | 0 problemas |

---

## 2. Tabla resumen por fase

| Fase | Qué se probó | Veredicto |
|---|---|---|
| **F1-A** · lote de pagos (13 commits del 26) | 28 comprobaciones | **27 PASA · 1 FALLA** (P-1) |
| **F1-B** · paquete de cuentas abiertas (27) | 43 comprobaciones | **40 PASA · 3 FALLA** (B-1, B-2, B-3) |
| **F2** · sedes, QR y correlativos | 16 comprobaciones | **15 PASA · 1 FALLA** (C-1) |
| **F3** · inventario y recetas por sede | 4 + **19 con el flag encendido** | **PASA** |
| **F4** · alerta de reposición | 5 comprobaciones | **PASA** |
| **F5** · proveedores y cuentas por pagar | 6 comprobaciones | **PASA** |
| **F6** · pedidos, mesoneros y roles | 12 comprobaciones | **PASA** |
| **F7** · cancelaciones | cubierto en F1-B (A8), F6 y F3 (V4) | **PASA** |
| **F8** · caja y cierre del día | 5 de lectura + **21 del cierre real** | **PASA** |
| **F9** · reportes y resumen del dueño | 9 comprobaciones | **8 PASA · 1 FALLA** (R-1, conocido) |
| **F10** · aislamiento total por sede | 27 comprobaciones | **26 PASA · 1 FALLA** (R-1, el mismo) |
| **F11** · avisos | cubierto en F4 y F1-B | **PASA** · el push quedó NO PROBADO (sin VAPID/suscripción viva) |

---

## 3. Bugs encontrados, por prioridad

Ninguno es de prioridad 1 (fuga entre sedes) ni de prioridad 2 (dinero equivocado). **Los busqué a fondo y no aparecieron** — el detalle de cómo se buscó está en §4 y §10.

### 🟠 P3 — un aviso que no llegaba

#### B-3 · El freno de "Pedir la cuenta" dejaba muda a la mesa de al lado — **ARREGLADO**

`/api/public/open-accounts/request-bill` limita a **6 peticiones por minuto**, y la llave del limitador es `ruta + IP` (`src/lib/rateLimit.ts:146`), **sin la mesa**. En un local todos los teléfonos salen por la misma IP del WiFi (y por CGNAT, incluso los que están en datos móviles pueden compartirla). Resultado: **6 peticiones por minuto para todo el restaurante**.

Comprobado: martillé el botón desde la mesa "A4" y a continuación una mesa distinta, con una cuenta distinta, pidió su cuenta y recibió **429**. El comensal ve "Ya avisamos al personal, espera un momento" y el mesonero nunca se entera.

- **Causa:** `src/lib/rateLimit.ts:145-146` (`key = id:ip`) usado desde `src/app/api/public/open-accounts/request-bill/route.ts`.
- **Arreglo aplicado:** ahora son **dos** frenos. Uno **por mesa** (6/min), que es el que importa — y la mesa se normaliza, así que `"MESA 3"`, `" mesa  3 "` y `"Mesa 3"` comparten cupo. Y uno **por IP** (60/min), tope generoso contra quien invente nombres de mesa para esquivarlo: un local de 6 mesas pidiendo 6 veces cada una son 36, cómodamente por debajo. De paso quedó bien el orden de los guards: origen y tamaño antes de tocar el body.
- **Verificado en vivo:** la mesa que martilla recibe 429 a partir de la sexta y **la mesa de al lado sigue respondiendo 200**. Test nuevo `requestBillRateLimit` con 6 casos, incluido el del local entero compartiendo la IP del WiFi.

### 🟡 P4 — se le imprime al cliente / validación que no está donde parece

#### B-1 · El marcador `[CUENTA_PEDIDA:…]` se imprimía en el ticket de 80 mm — **ARREGLADO**

"Pedir la cuenta" se hizo sin migración: la petición viaja como un marcador dentro de la nota de la cuenta. El panel la limpia con `stripBillRequestMarker`, pero **la API sirve la nota en crudo** y el ticket la pinta tal cual:

```
Nota de cuenta: Sin cebolla [pedido con corchetes] y mucha salsa [CUENTA_PEDIDA:2026-07-27T04:53:31.853Z]
```

- **Causa:** `src/app/local-santo/tickets/page.tsx:439` — `Nota de cuenta: {account.note}` sin limpiar.
- **Arreglo aplicado** (después de escribir este informe, en su propio commit): el ticket pasa la nota por `stripBillRequestMarker` antes de imprimirla. Es un cambio de presentación, sin lógica: la nota del cliente se sigue viendo entera, solo desaparece el marcador interno. Lo hice sobre la marcha porque dejaba un test rojo en la suite y porque lo que se imprimía iba a la mano del comensal.
- **Dónde SÍ está limpio** (revisado uno por uno): panel de cuentas abiertas, endpoint público `table-account-status` (solo expone `billRequestedAt`), comanda de cocina y ticket de pedido (usan `item.note`, que es otra cosa), CSV de cierres y de reportes (no incluyen la nota de la cuenta), historial de cierres y auditoría.
- **Reproducir:** `npm run qa:open-accounts`, bloque A2.
- **Fitness test:** `src/lib/__tests__/billMarkerNeverLeaks.fitness.test.ts` — recorre todo `src/` y falla si alguien vuelve a renderizar `*.note` de una cuenta sin pasarla por el helper. Falla hoy señalando exactamente ese archivo.
- **Confianza:** total.

#### P-1 · La regla de los 6 dígitos de referencia vive solo en el navegador

El formulario exige mínimo 6 dígitos (`MIN_REFERENCE_DIGITS` en `src/lib/paymentReportEvidence.ts`). **La API acepta `"4821"` y responde 201.** Lo mismo pasa con la exigencia de la foto del billete del efectivo en divisas que trajo `cbc0122`: es una guarda de `CartDrawer.tsx`, el servidor no la aplica.

Lo que el servidor **sí** valida, y funciona: sin captura *ni* referencia rechaza con 400; el mime y el formato del base64; el tamaño; el duplicado por tipo de pago.

- **Impacto real:** moderado-bajo. Caja revisa el comprobante a ojo antes de confirmar, así que no se traduce en dinero perdido. Pero un cliente que use el endpoint directamente puede dejar el pedido marcado como "reportado" con una referencia inventada.
- **Reproducir:** `npm run qa:payments`, bloque P3.
- **Confianza:** total.

#### B-2 · Quien pueda escribir la nota puede fingir que pidió la cuenta

Abrir una cuenta con la nota `[CUENTA_PEDIDA:2026-01-01T00:00:00Z]` enciende el badge del panel con esa hora inventada. Requiere credenciales de staff, así que la gravedad es baja.

**La buena noticia, que era el caso que más preocupaba:** un cliente **no** puede hacerlo desde el menú público. Lo intenté escribiendo el marcador en la nota de su pedido y la nota del pedido no toca la de la cuenta — el badge no se enciende. Ese bug no existe.

#### C-1 · Ninguna sede publica un número de WhatsApp

`mainWhatsapp` y `deliveryWhatsapp` están **vacíos en las dos sedes**, mientras el carrito muestra el botón "Enviar por WhatsApp". El cliente ve el botón y no tiene a dónde escribir. Es el hueco de configuración del dueño que ya estaba anotado; sigue abierto.

### 🔵 Anotaciones (ni bug ni bloqueo, pero conviene saberlo)

- **R-1 · El consolidado del dueño sigue sin desglose por sede.** `GET /api/reports?scope=all` da el total correcto (comprobado peso por peso: A $11 + B $22 == all $33) pero no dice cuánto puso cada sede. La causa exacta: `src/app/api/reports/route.ts:125`, la constante `SELECT_COLS` no incluye `branch_id`, así que agrupar es imposible sin tocarla. **Blind spot conocido, confirmado vivo.**
- **Los cruces entre sedes se rechazan con 500, no con 403/404.** El pedido de B pedido desde A, el insumo de B editado desde A: bloquean bien, pero el error sale como 500 con el mensaje crudo ("Ese insumo pertenece a otra sucursal"). Funciona; se ve como una caída.
- **Borrar un gasto de otra sede responde `200 ok:true` sin borrar nada.** El aislamiento aguanta (la fila sobrevive), pero la UI le diría al usuario "gasto eliminado".
- **Hay un contador de correlativos huérfano.** `order_branch_counters` tiene tres filas y una es de la sede `82e091c7-12a4-408c-8371-7747fb731b55`, que no está en `branches`. Resto de una sede borrada; inofensivo, pero es basura.

---

## 4. F1-B — el paquete de cuentas abiertas, atacado por donde no se probó

El paquete se dio por probado 14/14 en vivo. No repetí esas pruebas: fui a la concurrencia real, al marcador, al endpoint nuevo y al cableado con el resto del sistema. **40 de 43.**

**Lo que aguantó todo:**

- **Dos cobros concurrentes sobre la misma cuenta**, disparados en paralelo desde el script (no a mano): los dos entraron y el resultado fue la **suma exacta**. Cuenta de $100 con dos pedidos de $50, dos cobros de $30 a la vez → cobrado $60, pendiente $40, y lo cobrado en los pedidos cuadra al centavo con el total de la cuenta. El candado optimista de `e7878e6` hace lo que promete.
- **El marcador convive con notas complicadas.** Una nota que ya traía corchetes (`Sin cebolla [pedido con corchetes]`) sobrevive intacta; dos teléfonos pidiendo la cuenta a la vez **no** duplican el marcador y conservan la hora original; "Atendida" lo quita y deja la nota del cliente entera; cerrar la cuenta lo borra y no sobrevive en el historial.
- **Inyección desde el cliente: no existe.** Ver B-2.
- **Sondeo con 12 cuentas abiertas a la vez:** las 12 con su total exacto, ninguna perdió sus pedidos, 623 ms. Sobre el límite de mil filas de Supabase: el `.in()` de `loadOrderSummariesByAccount` (`src/lib/ordersStoreOpenAccounts.ts:96` y `:110`) no lleva `.limit()`, así que un truncado es teóricamente posible con muchísimos pedidos — pero **no tocaría el dinero**, porque los totales de la tarjeta salen de las columnas cacheadas de `open_accounts`, no de sumar los pedidos. Lo que se perdería es la lista de pedidos dentro de la tarjeta.
- **Cancelar recalcula al instante**, por los dos caminos: desde staff (100 → 40) y desde la cancelación pública del cliente (40 → 0).
- **`table_id`**: la cuenta en una mesa configurada guarda el vínculo (`table_id=barra`); con la config divergida (mesa que no existe) la cuenta **abre igual, sin vínculo y sin romperse**.
- **Cuenta cerrada:** cobrarla → 409; cerrarla otra vez → 409; pedir la cuenta de esa mesa → 404.
- **Agregar pedido:** en su sede nace atado, con la mesa correcta; apuntando a una cuenta de otra sede → 404.
- **Aislamiento:** la cuenta de A no aparece en el listado de B, y B no puede cobrarla ni cerrarla (404 en ambos). Sigue Abierta después de los intentos.

---

## 5. F1-A — el lote de pagos

**27 de 28.** Cada commit del lote, ejercitado con dinero real en la BD:

| Commit | Qué prometía | Resultado medido |
|---|---|---|
| `33bebbe` | confirmar dos comprobantes a la vez no borra dinero | **PASA** — dos confirmaciones en paralelo dejan $5 Zelle + Bs 1.400 = **$40 exactos**, pedido Pagado. Reconfirmar el mismo comprobante no duplica: lo frena el choque de moneda con un mensaje claro para caja |
| `8b3524f` | la 2ª pata no obliga a cuadrar a mano | **PASA** — la 1ª pata deja $5 y "Pago parcial"; la 2ª suma hasta $40 sola |
| — | protección contra pasarse del total | **PASA** — un comprobante que dejaría el pedido con más dinero del que vale se rechaza y explica por qué |
| `ad0e0bb` | en efectivo, la pantalla dejaba de pedir comprobante | **PASA** — el pedido 100% efectivo sale con `reportable: false` |
| `cbc0122` | "En revisión" se apaga al cobrar en caja | **PASA** — tras el cobro el cliente ve `confirmed: true` al instante, aunque el comprobante siga "En revisión" en la BD |
| `cbc0122` | el billete del efectivo en divisas es obligatorio | **PARCIAL** — la guarda existe en el checkout, no en el servidor (ver P-1) |
| `918e326` | el seguimiento mostraba el pedido viejo | **PASA** — dos pedidos del mismo teléfono con el viejo cancelado: el nuevo sale limpio, sin motivo de cancelación y con su propio número (`#128-s` vs `#129-s`) |
| `c3f40fe` | el "Listo" del delivery no manda a retirar | **PASA** — la API distingue `Delivery` de `Para llevar`, que es el campo con el que el cliente pinta el texto |
| `5a8b9ed` `d4ba28f` `5f139d6` `98b4990` | evidencia por pata, una tarjeta por pago | **PASA** — dos filas de comprobante, una por pata, y método único sigue aceptando solo captura |

**Regresiones que había que revisar:** método único no se endureció (con solo la captura reporta); los comprobantes de A no se ven, no se confirman y no se pueden adjuntar desde B; no quedó ninguna migración pendiente del lote.

---

## 6. F10 — matriz de aislamiento por sede

Cada módulo en cuatro columnas, con intentos **reales** de romperlo.

| Módulo | A ve solo A | B ve solo B | Dueño consolidado | Cruce forzado RECHAZADO |
|---|---|---|---|---|
| Pedidos | sí | sí | — | **sí** (PATCH y DELETE con el id de B desde A) |
| Reportes | sí | sí | **sí** (A+B == all, exacto) | — |
| Gastos del día | sí | sí | — | **sí** (el fix R4 de `deleteDayExpense` sigue tapado) |
| Inventario | sí | sí | — | **sí** (editar/vaciar el insumo de B desde A) |
| Proveedores y cuentas por pagar | sí | sí | — | **sí** (abonar una factura de B desde A: 0 abonos) |
| `/api/audit-logs` | sí | sí | — | — |
| `/api/payment-proofs` | sí | sí | — | **sí** |
| `/api/reservations` | sí | sí | — | — |
| `/api/surveys` | sí | sí | — | — |
| `/api/staff` | global **por diseño** | global | — | — |
| Menú público | sí | sí | — | **sí** |
| Cuentas abiertas | sí | sí | — | **sí** (cobrar y cerrar desde B: 404) |
| Cierres del día | sí | sí | **sí** | — |

**Suplantación a través del proxy** — esto es lo que no se puede probar inyectando headers desde el servidor, así que creé usuarios de staff reales, abrí sesión contra Supabase Auth y pegué a la API con el Bearer, como lo haría un atacante:

- Un **mesonero de San Diego** pidiendo `/api/orders` con el `x-branch-id` de Viñedo **no recibe ni un pedido de Viñedo**, y sigue viendo los suyos. El proxy clampa.
- Mandando `x-staff-role: owner` y `x-staff-all-branches: true` con su propio token **no se convierte en dueño**: 403.
- Con el id del pedido de la otra sede en la mano, el PATCH directo **no lo toca**.

**Blind spots verificados:** R2 descartado — 144 productos en la BD, **ningún id repetido entre sedes**. `order_items` sin `branch_id` no produjo ninguna fuga en las consultas ejercitadas.

---

## 7. F6 — roles, con sesiones de verdad

| El mesonero… | Resultado |
|---|---|
| abre una cuenta | **puede** (201) |
| le asocia un pedido | **puede** |
| cobra la cuenta | **403** — "caja debe registrar los cobros" |
| cierra la cuenta | **403** |
| marca "Atendida" la petición de cuenta | **puede** (200) — lo nuevo de `30a3aa7` funciona con su rol |
| cancela un pedido | **403**, y el pedido se queda en Nuevo |
| pide el consolidado del dueño | **403** |

---

## 8. F8 — el cierre del día, ejecutado de verdad

Con la confirmación de que los datos son de demo, cerré el día **de verdad** en San Diego. El script hace fotografía de los comprobantes antes, ejecuta el cierre, mide, y **restaura el estado exacto** al terminar (también si algo revienta a mitad). **21 de 21.**

Se montó un día completo: 3 pedidos ($50 + $30 + $20), cobro en caja de dos de ellos (uno en efectivo, otro en bolívares), un comprobante nuevo y un gasto de $12.

| Qué comprobé | Resultado |
|---|---|
| El cierre lleva la etiqueta de su sede | `branch_id` = San Diego |
| Aparece en el historial de A | sí |
| **Cerrar A no cierra B** | no aparece en el historial de B |
| El consolidado del dueño sí lo trae | 6 cierres (5 previos + el nuevo) |
| Archiva la fotografía del día | 54 pedidos y 24 comprobantes dentro del cierre |
| Limpia los comprobantes de su sede | A: 24 → 0 |
| **Aislamiento del borrado** | **B: 17 → 17, intactos** |
| El gasto queda marcado `Cerrado` | sí, con el id del cierre |
| Queda en auditoría con su sede | `day_close.saved`, sede correcta |

**El dinero cuadra:** lo cobrado en el cierre ($80) coincide con el reporte del día ($80); efectivo ($50) + equivalente en bolívares ($30) suman exactamente lo cobrado; el pendiente del cierre ($20) es el pedido sin cobrar. Y **cerrar el día no altera el reporte** — el cierre es una fotografía, no un borrado: antes $100, después $100.

**Cerrar dos veces:** el segundo cierre no re-abre ni duplica el gasto ya cerrado. El hallazgo abierto del QA del 12 de julio sobre "toggles de cierre que parecían decorativos" **queda descartado en lo que toca al dinero**: los gates de módulo fuerzan a 0 los campos de las secciones apagadas, y el marcado `Cerrado` de los gastos impide el doble descuento.

**Restauración verificada:** comprobantes A 23 → 23, B 17 → 17, historial de cierres 5 → 5, 0 pedidos y 0 gastos de prueba.

---

## 9. F3/F4/F5 — inventario, transferencias y alertas

El descuento automático vive **APAGADO** en la configuración real (`inventoryAutoDeductEnabled: false`, `inventoryAutoDeductDryRun: true`), y con esa config comprobé que un pedido con receta **no mueve el stock**, que es lo correcto. Pero con los datos confirmados como demo lo **encendí y lo probé de verdad**, devolviendo después las banderas a su valor exacto. **19 de 19.**

Un dato que conviene saber antes de nada: el descuento ocurre al **crear** el pedido (`src/lib/ordersCore.ts:93`), no al entregarlo.

| Qué comprobé, con el flag encendido | Resultado |
|---|---|
| Descuento exacto por receta | 3 hamburguesas → **6 panes** (3×2) y **3 de carne** (3×1), los dos insumos de la misma receta |
| Rastro | un movimiento `Consumo` por insumo, atado al id del pedido: `Consumo(-6)`, `Consumo(-3)` |
| **No cruza de sede** | el pedido de Viñedo descuenta 8 panes de Viñedo y **no toca ni un pan de San Diego** |
| Reenvío idempotente | el mismo `clientOrderId` dos veces descuenta **una sola vez** |
| Producto sin receta | no mueve nada de nadie |

**Lo más interesante — anular un pedido no devuelve el stock por sí solo, y está bien que así sea.** La devolución depende de que el staff declare si los ingredientes se usaron:

- Anulando **sin declarar nada**: el stock **queda descontado**. Tiene sentido: la comida ya se preparó.
- Anulando con `inventoryWasUsed: false`: **devuelve los 4 panes**, deja un movimiento `Ajuste(+4)` auditable, y escribe en la nota del pedido *"Ingredientes sin usar: se devolvieron N insumos al inventario"*.
- Anular dos veces **no** devuelve el stock dos veces.

Casi reporto el primer caso como bug. No lo es: es la decisión de negocio, y queda registrada por escrito en el pedido.

**Modo entrenamiento: NO PROBADO.** El módulo está apagado (`trainingModeModuleEnabled: false`), así que un pedido marcado `isTraining` no queda marcado como tal y descuenta normal. Para probarlo hay que encender el módulo primero.

**Lo demás de inventario:**

- **Transferencia entre sedes:** resta 20 en origen (50 → 30), suma 20 en destino, y deja **un movimiento en cada sede** ("Transferencia recibida +20" / "Transferencia enviada −20"). Un insumo de B **no** se puede transferir desde el contexto de A.
- **Alerta de reposición**, con las dos trampas resueltas: el disparo vive en el **GET** de `/api/orders` (`src/app/api/orders/route.ts:231`), no en el POST, y trae un throttle en memoria de 10 minutos por proceso. Con la marca de 24 h borrada, el latido escribe `inventory.restock.notified` con la sede correcta y el resumen del insumo (`1 bajo(s): …-BAJO (2/20 unidades)`), el panel del dueño lo cuenta, y el anti-spam evita el segundo aviso. **El push quedó NO PROBADO** por falta de VAPID y de una suscripción viva — es configuración pendiente, no un bug del módulo.
- **Cuentas por pagar:** abono parcial 40/100 → Parcial, pendiente 60; no deja pagar de más; el abono final marca Pagado y pendiente 0; y la compra a proveedor **no** se cuela como gasto del día.

---

## 10. F2 — sedes, QR y correlativos

- Cada sede sirve su configuración pública propia; la Mesa 3 se resuelve en las dos y sus cuentas son independientes.
- Un pedido creado con el `x-branch-id` de cada sede cae en la sede correcta y con la mesa correcta.
- **Correlativo por sede vivo:** el mismo instante produce `#140-s` en San Diego y `#63-v` en Viñedo, con contadores separados en `order_branch_counters`.
- **El índice 0033/0035 funciona de verdad:** la misma mesa ("mesa 1") está abierta **a la vez** en las dos sedes sin chocar, y la BD rechaza (`23505`) intentar abrir una segunda cuenta de esa mesa en la misma sede aunque se escriba `"  MESA 1  "`.

---

## 11. Scripts nuevos (todos commiteados y enganchados en `package.json`)

| Comando | Qué hace |
|---|---|
| `npm run qa:db-state` | estado real de las migraciones en la BD viva |
| `npm run qa:open-accounts` | F1-B: ataque al paquete de cuentas abiertas (43 comprobaciones) |
| `npm run qa:payments` | F1-A: el lote de pagos (28) |
| `npm run qa:isolation` | F10: matriz de aislamiento con cruces forzados (27) |
| `npm run qa:inventory` | F3/F4/F5: inventario, transferencias y alertas (20) |
| `npm run qa:roles` | F6/F9: roles reales por el proxy, con sesiones de Supabase Auth (18) |
| `npm run qa:branches` | F2/F8: sedes, QR, correlativos e historial de cierres (16) |
| `npm run qa:day-close` | F8: el cierre del día real, con fotografía y restauración (21) |
| `npm run qa:inventory-deduct` | F3: el descuento de inventario con el flag encendido (19) |

Todos siguen el molde del repo: cargan `.env.local`, montan por la API real, miden en la BD, limpian y salen con código ≠ 0 si algo falla. Y todos abortan si el servidor no es Brotherhood.

**Fitness test nuevo:** `src/lib/__tests__/billMarkerNeverLeaks.fitness.test.ts`.

---

## 12. Limpieza

Cada script verifica su propia limpieza al terminar. Estado final:

| Tabla | Filas `ZZTEST` |
|---|---|
| `orders` | **0** |
| `open_accounts` | **0** |
| `inventory_items` | **0** |
| `suppliers` | **0** |
| `supplier_purchases` | **0** |
| `menu_products` | **0** |
| `staff_profiles` (+ usuarios de Auth) | **0** |
| `day_expenses` | **0** |
| `supplier_purchases` | **0** |
| Usuarios en Supabase Auth | **0** de 3 (los 3 reales intactos) |

Dos avisos sobre la limpieza, porque casi cantan victoria en falso:

- **`day_expenses` guarda el concepto dentro de una columna JSON `data`**, no en una columna propia. Borrar filtrando por `concept` no borra nada y no da error: dos gastos de prueba sobrevivieron a la primera pasada. Ya están borrados y el script ahora filtra por el JSON.
- **No existe tabla `staff_profiles`**: los usuarios de staff viven en Supabase Auth. La limpieza ahora los borra de ahí y lo verifica con `listUsers()`.

En ambos casos el problema real era el mismo: un `DELETE` que falla en silencio y una comprobación que interpretaba el error como "0 filas". La limpieza de pedidos pasó a un helper compartido (`cleanupRunOrders`) que comprueba el error, reintenta y vuelve a contar.

No quedó ningún pedido de entrenamiento suelto. No se creó ninguna sede de prueba (ver §8). **El menú real quedó intacto: los 62 productos siguen ahí, sin cambios de precio, nombre, foto ni receta** — el único producto que creé fue `ZZTEST-…-HAMB`, borrado junto con su receta.

Lo único que queda pendiente de borrar está **fuera de este proyecto**: los residuos en la BD de Pizzería 007 descritos en §1, con su script listo.

---

## 13. Recomendaciones, en orden

~~1. Meter la mesa en la llave del freno de `request-bill` (B-3)~~ — **hecho**, con test y verificación en vivo.
~~2. Limpiar el marcador en el ticket de 80 mm (B-1)~~ — **hecho**, y el fitness test lo vigila.
~~3. Limpiar la base de Pizzería 007~~ — **hecho**, 0 residuos.
~~4. Probar el cierre del día real y el descuento de inventario~~ — **hecho**, 40 comprobaciones más, sin fallas.

Lo que queda, en orden:

1. **Poner los WhatsApp de las dos sedes** (C-1). Es configuración del dueño, no código: hoy el cliente ve el botón "Enviar por WhatsApp" y no hay número detrás.
2. **Subir las validaciones de pago al servidor** (P-1): los 6 dígitos de referencia y la foto del billete del efectivo en divisas. Hoy solo las aplica el navegador.
3. **Agregar `branch_id` a `SELECT_COLS` en `src/app/api/reports/route.ts:125`** y devolver `byBranch` en el consolidado (R-1). Requiere decidir la forma de la respuesta, así que no lo toqué.
4. **Devolver 403/404 en vez de 500** en los cruces entre sedes, y que el borrado cruzado de un gasto no responda `ok: true`. Bloquean bien; solo se ven como caídas.
5. **Encender el módulo de entrenamiento** si se quiere verificar que los pedidos de práctica no descuentan inventario ni cuentan en reportes. Hoy está apagado y un pedido marcado `isTraining` descuenta normal.
6. **Borrar el contador huérfano** de `order_branch_counters` (sede `82e091c7…`, que ya no existe).

El punto 3 toca la forma de una respuesta que consume el panel del dueño, así que conviene decidirlo antes de hacerlo.

---

## 14. La lección del prompt, aplicada

*"Desconfía de lo que no puedas ejercitar."* Tres cosas de esta ronda salieron precisamente de ahí:

- Los scripts e2e **pasaban** apuntando al servidor equivocado, escribiendo en la base de otro cliente. Verde no significa correcto.
- La alerta de reposición parecía rota hasta que encontré que el disparo vive en el GET y no en el POST. Un falso FALLO que casi reporto.
- Al revés: el marcador en el ticket **no** lo habría encontrado ninguna suite. Lo encontró abrir el archivo y leer qué se imprime.
